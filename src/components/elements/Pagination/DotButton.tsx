import * as React from 'react';

import { Button } from '@chakra-ui/react';

function DotButton() {
  return <Button isDisabled={true} variant="ghost">...</Button>;
}

export default DotButton;
